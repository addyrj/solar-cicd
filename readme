Here is your **ALL-IN-ONE PAGE DOCKER COMMANDS (PLAIN TEXT)** — no headings, no sections, just a clean single text file you can paste anywhere.

---

**ALL DOCKER COMMANDS (ONE PAGE TEXT)**
docker ps
Then stop all running containers:  docker stop $(docker ps -q)
Remove all containers:   docker rm $(docker ps -aq)
Remove related Docker images   docker images
Remove all images:   docker rmi $(docker images -q)
If it shows dependency errors: docker rmi -f $(docker images -q)
docker compose down -v



docker compose build --no-cache
docker compose up -d
docker-compose logs -f
docker-compose logs -f backend
docker-compose logs -f mysql
docker compose logs -f  frontend

docker logs solar_mysql --tail 100
docker logs solar_backend --tail 100
docker logs solar_frontend --tail 100





docker-compose down -v --rmi all --remove-orphans
docker system prune -af
docker exec -it solar_mysql mysql -u root -pyour_root_password
docker exec -it solar_mysql mysql -u root -pyour_root_password -e "USE iot_solar; SHOW TABLES;"
docker volume rm solar_client_mysql_data

# Recommended full reset:

docker-compose down -v --remove-orphans
docker system prune -af




4️⃣ Remove volumes (⚠️ deletes DB data)
Only run this if you don’t need MySQL data:
docker volume rm $(docker volume ls -q)
Or full cleanup:
docker system prune -a --volumes




